export default function Loading() {
  return (
    <div className="space-y-4">
      <div className="h-8 w-32 rounded bg-[var(--muted)] animate-pulse" />
      <div className="h-64 w-full rounded bg-[var(--muted)] animate-pulse" />
    </div>
  )
}
