.mds-loading,
.mds-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.mds-loading {
  padding: 80rpx 0;
  gap: var(--spacing-tight);
}

.mds-empty {
  padding: 60rpx 0;
}

.mds-text {
  font-size: 26rpx;
  color: var(--color-text-hint);
}
