# 凤御双美容院 — 客户端小程序产品需求规格书

> **文档版本**: 1.0.0
> **端口**: 客户端小程序（C端，appid: wx811eb4ded3dfba3f）
> **角色**: 顾客
> **约束文档**: `.42cog/real.md` v2.0.0 | `.42cog/cog.md` v2.0.0
> **参考设计稿**: `notes/客户端UI/`（MasterGo 原型，27 页截图）
> **日期**: 2026-03-09

---

## 1. 产品概述

**名称**: 凤御美业客户端小程序
**标语**: 一站式美容服务预约与消费平台
**目标用户**: 凤御双美容院的顾客（C端消费者）

**核心价值**:
1. 浏览并购买美容服务/产品，线上完成支付
2. 预约到店时间，查看服务进度与疗程余次
3. 扫码完成员工端开单的订单支付

**色彩方案**:
- 主色：暖金/橙色（品牌色）
- 价格色：红色
- 辅助色：绿色（加购按钮、状态标签）
- 背景：白色为主，浅灰色卡片/侧边栏

---

## 2. 核心用户旅程

### 旅程 A：顾客自助下单

```
浏览商品 → 选择 SKU → 选择美容师（可选）→ 提交订单 → 支付（微信/支付宝/线下）→ 订单完成
```

### 旅程 B：扫码支付（员工端开单）

```
微信扫码 → 自动加载订单 → 确认信息 → 选择支付方式 → 完成支付
```

### 旅程 C：预约 → 到店核销

```
疗程卡（剩余次数 > 0）→ 发起预约 → 员工确认 → 到店签到 → 服务完成（扣减次数）
```

---

## 3. 功能需求

### P0 — MVP 核心功能

#### 3.1 认证与用户身份

**实现状态**: 已实现

| ID | 需求 | 说明 |
|----|------|------|
| AUTH-01 | 微信静默登录 | `app.onLaunch()` 自动调用 `auth.login`（静默获取 openid）；新用户自动创建 `client_wechat_users` 记录（`user_id` 格式：`user_{timestamp}_{random}`）；先从 localStorage 恢复状态（快速），再与服务端同步（权威来源） |
| AUTH-02 | 手机号绑定 | 个人中心 `open-type="getPhoneNumber"` 按钮 → `wx.cloud.CloudID(cloudID)` → 云函数自动解密手机号；触发历史订单补全机制（按手机号匹配 `client_user_id IS NULL` 的订单），返回 `updatedOrdersCount`（已同步订单数量）；手机号已被其他用户绑定时返回 `INVALID_PARAMS: 该手机号已被其他用户绑定` |
| AUTH-03 | 门店绑定 | 从门店列表选择绑定默认门店，写入 `bound_store_name` + `bound_market_name`（从 WorkFine 自动提取） |

**实际认证流程**:
1. `app.onLaunch()` → `syncLoginState()` → `auth.login`（静默，无用户交互）
2. 返回 `{ isNewUser, userId, phone, boundStoreName, boundMarketName }`
3. 需要手机号时（如下单 -403 PHONE_REQUIRED）→ 个人中心绑定手机号
4. 绑定后清除服务端 auth 缓存（`invalidateAuthCache`），后续请求携带完整身份

> **设计稿方案（未实现）**: screen_002 设计稿描述了登录弹窗（欢迎文字 + 手机号输入 + 验证码 + 协议勾选 + 登录按钮），未勾选协议时按钮置灰。当前实现采用静默登录 + CloudID 手机号绑定方案，设计稿方案保留供后续参考。

**约束**:
- 手机号作为跨端身份标识（员工端开单时通过手机号匹配顾客）
- MVP 仅支持微信手机号快速验证（CloudID 解密），暂不支持短信验证码

---

#### 3.2 门店选择与绑定

**实现状态**: 已实现（含解绑流程）

| ID | 需求 | 说明 |
|----|------|------|
| STORE-01 | 门店列表 | 按市场分组展示营业中的门店（排除 `UDF_M_11956 = '是'`，排除"市场"和"管理中心"）；支持按城市筛选 |
| STORE-02 | 门店详情 | 展示门店名称、所属市场、可用床位、开业时间、`staff_count`（在职员工数）、`customer_count`（顾客数） |
| STORE-03 | 绑定门店 | 首次使用时选择绑定门店，后续默认展示该门店商品/美容师 |
| STORE-04 | 更换门店 | 支持申请解绑当前门店（解绑流程：发起申请 → 查看申请状态 → 可取消申请） |
| STORE-05 | 地理定位 | `store.geocode`（腾讯地图 API 逆地理编码，去掉"市"字后缀）→ 获取用户所在城市，辅助推荐就近门店 |

**门店列表搜索**: 需输入 ≥2 字符，按 `store_name` 或 `region` 匹配；不足 2 字符时显示提示

**绑定状态显示（4 种 UI 状态）**:

| 状态 | UI 显示 |
|------|---------|
| `no-binding` | "绑定门店后..."提示 + "绑定此门店"按钮 |
| `is-current` | "当前门店"标签 + "申请解绑此门店"按钮 |
| `is-current-reviewing` | "解绑申请审批中..."提示 + 禁用按钮 + "取消申请"危险按钮 |
| `other-bound` | "已绑定「{storeName}」，请先解绑"（禁用） |

**解绑流程**: 输入解绑原因/备注（textarea popup）→ `store.requestUnbind({ note })` → 状态变为 `is-current-reviewing` → 可取消申请

**绑定门店页 UI（screen_002 下半部分）**:
- 页面标题"请选择绑定门店"
- 门店选择：下拉单选
- 底部"确定"按钮

**数据来源**: WorkFine `UDT_M_219`（只读）

**API**: `store.list` / `store.detail` / `store.requestUnbind` / `store.getUnbindRequest` / `store.cancelUnbindRequest` / `store.geocode`

---

#### 3.3 首页

**实现状态**: 已实现

**导航栏**: 品牌 Logo「FENGYU 凤御美业」+ 门店名称选择器（显示 `boundStoreName || '选择门店'`）+ 扫码按钮（`wx.scanCode()`）

**页面结构（screen_003/004）**:

| 区域 | 内容 | 说明 |
|------|------|------|
| 搜索栏 | 搜索输入框，支持跨分类即时搜索（按 SPU 名称过滤，搜索结果替代商品列表区） | 已实现 |
| Banner 轮播 | 大幅横版广告图（5 张 CDN 图片），支持左右滑动 + 圆点分页指示器，自动轮播 4s/张 | 图片可配置 |
| 快捷入口（金刚区） | 5 个圆形图标：福利活动 / 护理项目 / 家居产品 / 我的券 / 我的疗程 | 详见下方说明 |
| 产品专区 | 左侧分类导航（二级层级）+ 右侧商品卡片列表（联动滚动） | 与商品浏览页共用分类逻辑 |
| 购物车 FAB | 右下角浮动按钮，badge 显示已选商品数量（>99 显示 `99+`），仅 `cartCount > 0` 时可见 | 跳转购物车页面 |

**快捷入口行为**:

| 按钮 | 行为 |
|------|------|
| 福利活动 | 滚动左侧栏到"福利活动"分类并加载 |
| 护理项目 | 滚动左侧栏到"护理项目"分类并加载 |
| 家居产品 | 滚动左侧栏到"家居产品"分类并加载 |
| 我的券 | Toast 提示"优惠券功能开发中"（P1） |
| 我的疗程 | 跳转疗程卡页面（treatment-cards） |

**扫码入口**: `wx.scanCode()` → 解析二维码内容 → path 型直接 `navigateTo` / 纯文本 orderNo 型跳转 scan-pay 页面

**分类层级说明**:
- `big_category` 映射：`促销方案 → 福利活动`、`生美/非生美 → 护理项目`、`院装产品 → 家居产品`
- 左侧栏显示 big_category 标题（不可点击）+ 下级 `category` 子分类（可点击切换）
- 仅显示含有效 SKU 的分类

**SPU 列表加载**: 按分类分页加载（scroll-to-lower 触发，500ms 防抖），带 `_spuCache` 模块级缓存；搜索时预加载所有未缓存分类后按名称过滤

**切换门店**: 切换 `boundStoreName` 时清空购物车和 SPU 缓存，防止跨门店商品不一致

**商品卡片**: 商品封面图 + 名称 + 红色价格（`¥X 起`）+ 绿色"购买"按钮；推荐商品显示"推荐"标签

---

#### 3.4 商品浏览与选购

**实现状态**: 已实现

**三层浏览结构**:

| 层级 | 页面 | 说明 |
|------|------|------|
| 一级 | 左侧品项分类选择器 | 从 `product_spu.category` 动态派生，仅显示含有效 SKU 的分类；院装产品固定追加在末尾 |
| 二级 | 右侧 SPU 卡片列表 | 同一商品不同规格合并为一张卡，展示封面图、名称、价格起步、生美/非生美标签 |
| 三级 | 商品详情页（SKU 规格选择） | 展示全部 SKU 规格选项（规格名 + 价格 + 疗程服务次数），选择后可下单 |

**分类查询逻辑**:
```sql
SELECT p.category, MIN(p.sort_order) AS category_order
FROM product_spu p
WHERE p.big_category != '院装产品'
  AND EXISTS (
    SELECT 1 FROM product_spu_sku_map m
    WHERE m.spu_id = p.spu_id AND m.is_active = true
  )
GROUP BY p.category
ORDER BY category_order ASC;
-- 院装产品节点固定追加在末尾
```

**商品详情页 UI（service-detail）**:
- 封面图（全宽）或占位图标
- 商品名称、描述、分类标签（按 big_category 着色：紫/主色/青/橙）
- 价格显示：`¥{selectedSku.price}` + `/ {session_count} 次`（疗程卡类）
- SKU 规格选择：药丸形按钮网格（规格名 + 价格 + 次数），选中高亮，切换时重置数量为 1
- 数量调节器：min 1, max 99，仅 SKU 选中后显示
- 美容师选择 Popup：底部弹出列表，含"不指定"选项 + 门店美容师列表（姓名 + 职位）
- **操作栏（sticky）**:
  - 常规商品：左侧价格 `¥{price} ×{qty}` + 购物车 FAB（带 badge）+ "加入购物车"按钮 + "立即下单"按钮
  - 促销方案商品（`big_category = '促销方案'`）：隐藏购物车按钮和"加入购物车"，仅显示全宽"立即下单"
- 促销方案商品详情页额外展示 `promotionSchemeId`（关联 WorkFine `UDT_S_1459` + `UDT_M_1460`）

**购物车机制（localStorage）**:
- `utils/cart.ts` 提供：`addToCart` / `removeFromCart` / `updateQuantity` / `clearCart` / `getCartCount` / `getCartTotal`
- CartItem 结构：`{ skuId, spuId, spuName, skuDisplayName, coverImage, price, quantity, bigCategory, productType, addedAt }`
- 购物车页面（`pagesShop/shopping-cart`）：全选/单选 checkbox、数量 stepper（min 1, max 99）、删除、总价计算（仅已勾选项）、"结算(n)"按钮 → 存 `checkoutItems` 到 localStorage → 跳转 `checkout?fromCart=1`
- 空状态：居中 van-empty + "去逛逛"按钮

**商品数据来源**:

| 商品类型 | WorkFine 表 | PG 表 |
|----------|------------|-------|
| 可售服务项目（全国） | `UDT_M_1281`（481 条） | `product_spu` + `product_spu_sku_map` |
| 门店自定义项目 | `UDT_M_1383`（401 条） | 同上 |
| 院装产品 | `UDT_M_341`（1,940 条） | 同上 |
| 促销方案 | `UDT_M_1460`（方案项目） | 同上 |

- SPU 元数据（名称、图片、描述、分类、排序）存 PG `product_spu`
- SKU↔WorkFine 映射存 PG `product_spu_sku_map`（含 `market_restriction` 市场限定字段）
- SKU 价格/次数运行时从 WorkFine 读取，带 5 分钟模块级缓存（Map 结构，key: `{workfine_source}:{workfine_item_id}`）

**API**: `product.categories` / `product.spuList` / `product.skuDetail` / `product.spuDetail` / `product.hotList` / `product.shopInit`

---

#### 3.5 美容师浏览

**实现状态**: 已实现（Popup 交互）

| ID | 需求 | 说明 |
|----|------|------|
| STAFF-01 | 美容师列表 | 按绑定门店过滤在职美容部员工（`department = '美容部'` OR `position = '美容师'`） |
| STAFF-02 | 默认美容师 | 顾客档案中的主美容师（`UDT_S_311.UDF_S_6444`），预约/下单时默认填充 |
| STAFF-03 | 选择非必须 | 下单时可不指定美容师（"不指定"选项） |

**当前实现（Popup 交互）**:
- 美容师选择以底部 Popup 弹窗实现，出现在商品详情页、结算页、创建预约页
- Popup 内容：列表含"不指定"选项 + 门店美容师列表（姓名 + 职位）
- 点击选中后关闭 Popup，预填到对应字段

**独立技师列表页 UI（screen_008，设计目标）**:
- 圆形头像 + 姓名 + 职称
- 所属门店

> 设计稿描述了独立技师列表页（screen_008），当前以 Popup 弹窗实现作为过渡方案。

**查询条件**: 在职（`UDF_S_1624 = '否'`）+ 属于已选门店 + 美容部员工（`department = '美容部'` OR `position = '美容师'`）

**数据来源**: WorkFine `UDT_S_287`（只读）

**API**: `staff.list`（返回 staff_id, name, store_name, position, department, phone）/ `staff.default`（返回 mainStaffId, mainStaffName, mainStaffPosition）

---

#### 3.6 下单与支付

**实现状态**: 已实现

**结算页三种入口模式**:

| 模式 | 触发方式 | 数据来源 |
|------|----------|----------|
| 单品直接下单 | 商品详情页"立即下单" → URL 带 `skuId` + `quantity` + `staffWfId` 等参数 | `product.skuDetail(skuId)` 获取价格 |
| 购物车结算 | 购物车页"结算" → URL 带 `fromCart=1` | 读取 `localStorage['checkoutItems']`，多商品显示"N 件商品" |
| 恢复支付 | 订单详情页"去支付" → URL 带 `orderNo` | `order.detail(orderNo)` 加载已有订单 |

**促销方案下单**: `orderType=promo` + `promotionSchemeId`，促销方案不进购物车

**订单确认页 UI（screen_011）**:
- 服务信息：SPU 名称 + SKU 规格名
- 指定美容师（可选，Popup 选择）
- 门店（不可编辑，从 `store.list` 获取 `marketName`）
- 消费协议复选框（未勾选时禁用提交按钮）
- 支付方式选择（微信支付 / 支付宝 / 线下付款）
- 价格明细：商品单价 + 数量 + 优惠（当前 ¥0.00）+ 实付金额
- 底部提交栏：按钮文本随支付方式变化（"微信支付" / "支付宝付款" / "提交订单"）

**手机号绑定拦截**: `order.create` 返回 `-403 PHONE_REQUIRED` → 弹出绑定 overlay → `open-type="getPhoneNumber"` → CloudID 绑定手机号 → 绑定成功后自动重试下单

**支付方式**:

| 方式 | 流程 | 状态变化 | 当前实现 |
|------|------|----------|----------|
| 微信支付 | `order.pay(orderNo)` → 获取 `paymentParams` → `wx.requestPayment()` → 回调成功 | 待支付 → 已支付 | mock 模式（返回模拟 prepay_id，待接入真实微信支付） |
| 支付宝 | `order.alipayPay(orderNo)` → 获取 QR 码 URL → 全屏 Popup 展示金额 + "我已完成支付"按钮 | 待支付 → 已支付 | mock 模式（返回模拟 QR URL，待接入真实支付宝） |
| 线下付款 | `order.offlinePay(orderNo)` → Toast "已提交，等待店长确认收款" | 待支付 → 待确认收款 → 已支付 | 已完整实现 |

**10 分钟订单超时机制**:
- 订单创建时计算 `expire_at = created_at + 10min`
- 过期订单自动清理（在 `order.list` / `order.create` / `order.pay` 时触发懒清理）
- 创建新订单时同时清理该用户旧的过期待支付订单

**核心规则**:
- 提交防重：前端防连点 + 后端幂等（部分唯一索引 `UNIQUE (client_user_id) WHERE status = '待支付'`）
- 价格快照：开单时从 WorkFine 读取 SKU 价格写入 `order_items.unit_price`，后续不可变
- 订单进入 `已支付` 后，若指定了美容师，系统自动创建营业额分配记录
- 订单号格式：`FY-XSD-WX-{YYMMDD}{4位序号}`
- 明细行流水号格式：`XSLSH-WX-{YYYYMMDD}{4位序号}`（advisory lock 防并发）
- 顾客姓名回填：从 `UDT_S_311` 按手机号查询 `UDF_S_1476`

**订单状态集**: `待支付` / `待确认收款` / `已支付` / `已完成` / `支付失败` / `已关闭`

**API**: `order.create` / `order.pay` / `order.alipayPay` / `order.offlinePay`

---

#### 3.7 订单管理

**实现状态**: 已实现

**订单列表页 UI（screen_020/021/022）**:
- Tab 筛选栏：全部 / 待支付 / 已支付 / 已完成
- 订单卡片：
  - 头部行：订单号（左）+ 体验单显示"体验"标签（`order_type === '体验'`）+ 状态标签（右，带颜色：待支付/橙、待确认/棕、已支付/绿、已完成/灰、支付失败/红、已关闭/灰）
  - 商品行：`{spu_name} · {sku_display_name} ×{qty}` + `剩余{remaining_sessions}次`（疗程卡类）
  - 底部：下单日期（左，`YYYY-M-D` 格式）+ "合计 ¥{total_amount}"（右）
  - 待支付订单显示"去支付"按钮
- 空状态："暂无订单记录"

**各状态特殊操作**:

| 状态 | 顾客可执行操作 |
|------|--------------|
| 待支付 | 去付款、取消订单 |
| 待确认收款 | 等待（店长确认中） |
| 已支付 | 查看详情、查看套餐明细（展开子项目）、发起预约（仅有可预约项时） |
| 已完成 | 查看详情 |
| 支付失败 | 等待（店长重置后可重新付款） |
| 已关闭 | 查看详情 |

**订单详情页**:
- 状态 hero 区：大图标 + 状态文字 + 状态提示
  - 待支付：显示过期时间 + 倒计时（MM:SS 格式，1 秒刷新，超时后自动重载）
  - 待确认收款：显示"请等待店长确认收款"
- 订单信息：订单编号、下单时间（`YYYY-MM-DD HH:MM`）、支付方式、门店、指定美容师（可选）
- 商品明细卡片（逐行）：
  - 名称 + 规格 + ×数量 + 价格 `¥{receivable}`
  - 疗程卡/单品：显示"剩余 X/Y 次"格式 + 有效期至日期
  - 院装产品：显示"已交付"标签
- 价格汇总：商品总价 + 优惠（当前 ¥0.00）+ 实付金额
- **底部按钮组合**（按状态）：
  - 待支付："取消订单"（plain）+ "去支付"（primary）
  - 已支付且有可预约项："发起预约"（primary）→ 跳转疗程卡页
  - 终态（已完成/已关闭/支付失败/待确认收款/已支付无可预约项）："返回首页"（plain primary）

**可预约项目查询**: 过滤订单中 `remaining_sessions > 0` 且非院装产品的已支付订单行；支持 `includeInactive` 参数返回已用完/已过期项

**API**: `order.list` / `order.detail` / `order.cancel` / `order.appointableItems`

---

#### 3.8 预约管理

**实现状态**: 已实现

**预约入口**:
- 底部「预约」Tab → 预约列表页 FAB"发起预约"按钮
- 疗程卡页面（treatment-cards）→ 点击疗程卡"预约"按钮
- 订单详情页"发起预约"按钮 → 跳转疗程卡页面（非直接创建预约）

**发起预约（screen_009/010）**:

| ID | 需求 | 说明 |
|----|------|------|
| APPT-01 | 入口 | 预约列表页 FAB 或疗程卡页面"预约"按钮 |
| APPT-02 | 选择项目 | 内联可选列表（radio icon 切换 circle/success），展示可预约的疗程卡/单品行；支持"不关联疗程卡"选项（`itemFlowNo` 为空时 `service_name` 显示为"到店预约"） |
| APPT-03 | 选择美容师 | 可选，底部 Popup 弹窗，含"不指定"选项 + 门店美容师列表 |
| APPT-04 | 选择时间 | 日历组件（当前日期到 +90 天）+ 5 个固定时段 selector（09:00-11:00 / 11:00-13:00 / 13:00-15:00 / 15:00-17:00 / 17:00-19:00） |
| APPT-05 | 备注 | 多行文本输入（选填，max 200 字符，带字数统计） |

**预约时间格式**: `"YYYY-MM-DD HH:MM-HH:MM"`（如 `"2026-03-15 09:00-11:00"`）

**预约 ID 格式**: `apt_{timestamp}_{random}`

**预约列表（预约 Tab）**:
- Tab 筛选：全部 / 待确认 / 已确认 / 已取消
- 卡片 UI：头部（服务名 + 状态标签，按状态着色）+ 单元格（预约时间 `M月D日 H:MM` / 美容师 / 来源订单）
- 操作：待确认/已确认状态显示"取消预约"按钮
- 预约列表 LEFT JOIN `order_items` + product 获取 `service_name`、`sku_display_name`

**取消预约**: 顾客可取消 `待确认` 或 `已确认` 状态的预约，可填写取消原因

**约束规则**:
- 只有 `已支付`（含体验单）且 `remaining_sessions > 0` 的订单行可发起预约
- 院装产品不可预约
- 同一订单行已有 `待确认` 或 `已确认` 预约时，不可重复发起
- 预约取消后（`已取消`）可重新发起；系统关闭的（`已关闭`）不可重新发起
- 预约不扣减次数（次数在服务完成时由员工端扣减）

**API**: `appointment.create` / `appointment.list` / `appointment.cancel`

---

#### 3.9 到店服务（顾客视角）

**实现状态**: 已实现（只读查看）

顾客端为服务单的只读视角：
- 查看服务进行状态：待服务 / 服务中 / 已完成
- 服务信息：服务日期、服务时长、服务美容师、关联项目
- 疗程卡类在服务完成后自动更新剩余次数

**产品类型差异**:

| 产品类型 | session_count | 服务流程 |
|----------|--------------|----------|
| 疗程卡 | ≥ 2 | 多次到店核销，次数归零后订单行完成 |
| 单品 | 1 | 一次到店核销，服务完成后该行完成 |
| 院装产品 | null | 支付即完成，不走到店服务流程 |

**API**: `service.detail`

---

#### 3.10 扫码支付

**实现状态**: 已实现

**扫码流程（旅程 B）**:
1. 员工端开单后生成二维码
2. 顾客微信扫码 → 打开客户端小程序
3. QR code 解析：path 型（直接 `navigateTo`）vs 纯文本 orderNo 型（跳转 scan-pay 页面）
4. 自动加载订单信息（`order.scanDetail`，仅限 `order_source='staff'` 的订单）
5. 展示订单详情 → 确认付款
6. 选择支付方式 → 完成支付

**scan-pay 页面**:
- 订单头部：门店名 + 状态标签
- 商品明细行：名称 + 规格 + 价格（右对齐）
- 订单总价（大字加粗）
- 支付方式选择（微信支付 / 支付宝 / 线下付款，与 checkout 一致）
- 底部提交栏（Vant submit-bar）

**错误状态处理**:
- 非待支付状态返回 `statusMsg`（已支付/已关闭等）→ 显示信息浮层，不返回明细
- 无效二维码 / 订单不存在 → 友好提示

**约束**: 员工端开单时订单已写入数据库（状态 `待支付`），扫码后无需重复创建；`scanDetail` 无需用户认证（公开接口）

**API**: `order.scanDetail` / `order.pay` / `order.alipayPay` / `order.offlinePay`

---

#### 3.11 疗程卡管理

**实现状态**: 已实现（treatment-cards 页面）

- 展示顾客持有的所有疗程卡（已支付订单中的疗程卡/单品行）
- 每张卡显示：项目名称、剩余次数/总次数、到期日、状态
- 快捷入口：从卡片直接发起预约

**到期日规则**:
- 疗程卡：开单时写入合同约定到期日
- 单品：支付成功时系统写入 `paid_at + 1 year`

---

#### 3.12 个人中心

**实现状态**: 部分实现

**已实现部分**:
- 头像区：联系人图标（青色）+ 脱敏手机号（`138****7038` 格式，未绑定显示"未绑定手机号"）+ 绑定门店名（带定位图标）
- 快捷导航：我的订单（→ orders 页面）、我的疗程卡（→ treatment-cards 页面）
- 手机号绑定：`open-type="getPhoneNumber"` 按钮 → CloudID 绑定 → 成功提示"已同步 N 笔历史订单"或"绑定成功"
- 切换门店：跳转 store-select 页面
- 版本号显示：凤御美容 v1.0.0

**设计稿更丰富部分（待实现）**:

| 区域 | 内容 | 实现状态 |
|------|------|---------|
| 用户信息增强 | 头像可更换 + 昵称 + 会员等级"尊金会员" + 右箭头编辑入口 | 未实现 |
| 四宫格快捷功能 | 优惠券 / 积分 / 消息 / 我的评价 | 未实现 |
| 订单快捷入口 | 两行按钮网格：待付款 / 已付款 / 已预约 / 待服务 / 已服务 / 待评价 / 已评价 / 已取消 / 退款售后 / 全部订单 | 未实现（当前为简化的列表入口） |
| 其他入口 | 服务记录 / 我的卡包 / 充值卡 / 意见反馈 / 客服热线"400-000-0000" + "在线客服"按钮 | 未实现 |

---

### P1 — 重要功能（v1.0）

#### 3.13 个人资料编辑

**实现状态**: 未实现

**个人信息编辑页 UI（screen_025）**:
- 导航栏标题"设置个人资料"，右侧"保存"按钮
- 可编辑：头像（可更换）、名称
- 只读：编号（系统生成）、注册门店
- 联系电话 + "换绑"按钮

| ID | 需求 | 说明 |
|----|------|------|
| PROFILE-01 | 修改头像 | 从相册选择或拍摄 |
| PROFILE-02 | 修改昵称 | 文本输入 |
| PROFILE-03 | 换绑手机号 | 重新调用 `getPhoneNumber` 获取新手机号 |

---

#### 3.14 来源渠道绑定

**实现状态**: 未实现（需扩展 bindStore 接口）

**绑定门店页增强（screen_002 下半部分）**:
- 来源选择（下拉单选）：推广部 / 老带新 / 美团 / 抖音 / 转让店 / 自进 / 内部地推 / 第三方拓客
- 宣传推广人（文本输入，选填）

需在 `client_wechat_users` 表或独立表中增加 `source_channel`、`promoter` 字段。

---

#### 3.15 优惠券

**实现状态**: 未实现

**优惠券页 UI（screen_026）**:
- 导航栏标题"我的优惠券"
- Tab 切换：未使用 / 已使用 / 已过期
- 兑换码输入区：输入框 + "兑换"按钮
- 优惠券卡片：金额（¥15）+ 使用门槛（满 ¥15 可用）+ 券名称 + 有效期 + 适用范围

**券种类型**（来自会议纪要 2026-03-04）:

| 券种 | 本质 | 说明 |
|------|------|------|
| 现金券 | 直接抵扣金额 | 从促销方案附带赠送 |
| 项目券 | 限定范围的现金券 | 限定在特定品项分类中使用 |
| 折扣券 | 乘法计算 | 逻辑最复杂，后续迭代 |

通用设计要素：满减条件、适用范围、适用门店。

**下单关联**: 订单确认页展示可用优惠券，选择后计入价格明细。

---

#### 3.16 门店详情增强

**实现状态**: 部分实现（基础信息已有，增强内容未实现）

**门店列表增强（screen_016）**:
- 搜索栏：关键词搜索门店
- 排序栏：距离 / 评分 / 销量（升降序）
- 门店卡片增强：门店图片、评分、距离、月售、营业时间、地址

**门店详情增强（screen_014）**:
- 门店主图 + 营业时间 + 地址 + 电话 + 评分 + 月售
- 全部商品区（横向卡片）
- 推荐技师区（头像 + 姓名）
- 用户评价区（依赖评价系统，P2）

---

#### 3.17 美容师详情增强

**实现状态**: 未实现

**技师卡片增强（screen_008）**:
- 状态标签：空闲/忙碌（通过查询当日有效预约动态计算）
- 擅长项目（文本描述）
- 评分 + 已服务次数
- 右侧"预约"按钮（跳转预约页并预填美容师）

**筛选排序**: 服务数量（下拉）、擅长项目（下拉）

---

#### 3.18 服务记录

**实现状态**: 未实现（需新增列表查询接口）

**服务记录页 UI（screen_027 子页面）**:
- 导航栏标题"服务记录"，右上角筛选
- 记录卡片：服务项目名 + 护理地点 + 技师名 + 服务时间 + 服务时长

入口：个人中心 → 服务记录

---

#### 3.19 凤御馆（品牌介绍）

**实现状态**: 已实现（Tab3，内容待完善）

**当前实现**: 单张品牌宣传 CDN 图片，纯展示页。

> **规划内容（待完善）**: 设计稿（screen_015）描述了品牌介绍长图文页面：品牌简介 → 专家团队介绍（头像 + 简介）→ 资质证书展示，纵向滚动浏览。

> 设计稿高保真版将 Tab3 改为"消息"，但当前实现保留为"凤御馆"。消息中心移至 P2。

---

### P2 — 增强功能（Future）

#### 3.20 积分系统

**积分首页 UI（screen_018）**:
- VIP 会员卡片（红色渐变）：VIP 字样 + 会员等级 + 当前积分
- "查看积分明细 >"入口
- 积分商品列表（两列网格）：商品图 + 所需积分
- 筛选栏：综合排序 / 兑换商品 / 兑换券 / 筛选

**积分明细页**:
- 积分总额
- Tab 切换：积分收支明细 / 积分兑换明细
- 二级筛选：全部 / 已获得 / 已消耗

需新建积分相关数据表，对接会员等级体系。

---

#### 3.21 评价系统

**评价列表 UI（screen_023）**:
- Tab 切换：待评价 / 已评价
- 评价卡片：项目名 + 预约时间 + 服务时间 + 门店 + 技师
- 待评价：显示"去评价"按钮；已评价：显示"详情"按钮

**评价提交页**:
- 服务信息展示
- 四维星级评分（各 1-5 星）：门店环境 / 服务态度 / 服务流程 / 技师专业
- 评价内容文本输入
- "提交"按钮

---

#### 3.22 消息中心

**消息页 UI（screen_024）**:
- 消息列表（IM 列表样式）：头像 + 名称 + 摘要 + 时间 + 未读角标
- 消息类型：
  - 服务消息（蓝色图标）：预约提醒、支付通知
  - 系统消息（绿色图标）：优惠通知
  - 用户私信（真人头像）：与技师/客服的对话

如启用消息中心，TabBar 将更新为：首页 / 预约 / 消息 / 我的。

---

#### 3.23 在线客服

**聊天页 UI（screen_019）**:
- 导航栏标题为门店名称
- IM 聊天界面：时间分隔线 + 客服消息（左/灰色气泡）+ 用户消息（右/白色气泡）
- 底部输入栏：文本输入框 + 发送按钮

---

#### 3.24 卡包与充值卡

**我的卡包页**:
- 卡片展示：卡名称 + 金额 + 到期日

**充值卡页（screen_027 子页面）**:
- 卡内余额大字显示
- 筛选：门店下拉 + 日期范围选择器
- 消费明细列表：类型（充值/消费）+ 备注 + 时间 + 金额
- 底部提示："仅显示最近半年明细，只限持卡期间交易"

---

#### 3.25 全局搜索

- 搜索框支持关键词搜索：商品名、服务项目名、门店名
- 搜索结果分类展示
- 搜索历史 + 热门搜索推荐

---

#### 3.26 意见反馈

- 表单提交反馈内容（文本 + 图片）
- 入口：个人中心 → 意见反馈

---

#### 3.27 发票

- 下单时选择发票类型（个人/企业）
- 填写发票信息（抬头、税号等）
- 已开发票记录查看

---

## 4. 状态机

### 4.1 订单状态机

```text
待支付 → 已支付              （微信/支付宝支付回调成功）
待支付 → 待确认收款          （顾客选择线下付款提交）
待支付 → 支付失败            （支付超时/失败）
待支付 → 已关闭              （顾客取消或手动关闭）
待确认收款 → 已支付          （店长确认线下收款）
支付失败 → 待支付            （店长手动重置，允许重新付款）
已支付 → 已完成              （所有疗程卡/单品行 remaining_sessions 归零；院装产品支付即完成）
```

**补充说明**:
- `待确认收款`：仅用于线下付款的中间状态
- 体验单（`order_type = 体验`）与正式订单走相同的状态机
- 订单关闭/支付失败时，对应的营业额分配记录标记为无效（`is_void = true`）

### 4.2 预约状态机

```text
待确认 → 已确认              （员工确认预约）
待确认 → 已取消              （顾客取消）
已确认 → 已取消              （顾客取消）
已确认 → 已完成              （关联的服务单完成后自动流转）
待确认 → 已关闭              （超期未到店 或 该订单行剩余次数归零）
已确认 → 已关闭              （超期未到店 或 该订单行剩余次数归零）
```

**`已关闭`触发条件**（二选一）:
1. 定时任务：预约时间超过 1 天未到店（`appointment_time < NOW() - INTERVAL '1 day'`）
2. 服务完成：该订单行 `remaining_sessions` 归零时，关联的 `待确认`/`已确认` 预约自动关闭

**取消规则**: `已取消`状态可重新发起新预约；`已关闭`状态不可重新发起。

### 4.3 服务单状态机（顾客端只读）

```text
待服务 → 服务中 → 已完成
```

- 状态推进由员工端操作，顾客端仅查看
- 仅在 `服务中 → 已完成` 时扣减次数，幂等处理

---

## 5. 页面结构与导航地图

### 5.1 TabBar

| Tab | 页面路径 | 说明 |
|-----|----------|------|
| 首页 | `pages/home/home` | 主页面 |
| 预约 | `pages/appointment/appointment` | 预约列表 |
| 凤御馆 | `pages/cart/cart` | 品牌介绍（MVP）；P2 改为消息中心 |
| 我的 | `pages/profile/profile` | 个人中心 |

### 5.2 分包结构

| 分包 | 页面 | 说明 |
|------|------|------|
| pagesShop | shop（商品分类浏览）、service-detail（商品详情/SKU选择）、shopping-cart（已选商品） | 商品模块 |
| pagesOrder | orders（订单列表）、checkout（下单确认）、order-detail（订单详情）、scan-pay（扫码支付）、treatment-cards（疗程卡列表） | 订单模块 |
| pagesStore | store-select（门店选择）、store-detail（门店详情） | 门店模块 |
| pagesAppointment | appointment-create（创建预约） | 预约模块 |

### 5.3 导航地图

```
TabBar
├── 首页 (home)
│   ├── 搜索 → 跨分类即时搜索（按 SPU 名称过滤，结果替代商品列表区）
│   ├── 门店选择器 → 门店列表 (store-select) → 门店详情 (store-detail)
│   ├── 扫码入口 → wx.scanCode → scan-pay（orderNo 型）或 navigateTo（path 型）
│   ├── Banner → 活动详情
│   ├── 福利活动 / 护理项目 / 家居产品 → 滚动到对应分类
│   ├── 我的券 → Toast "开发中" (P1)
│   ├── 我的疗程 → 疗程卡列表 (treatment-cards)
│   ├── 商品卡片 → 商品详情 (service-detail) → 加入购物车 / 立即下单 → 结算 (checkout)
│   └── 购物车 FAB → 购物车页 (shopping-cart) → 结算 (checkout?fromCart=1)
├── 预约 (appointment)
│   ├── 预约列表（Tab 筛选：全部/待确认/已确认/已取消）
│   └── FAB "发起预约" → 创建预约 (appointment-create)
│       ├── 选择项目（内联列表，含"不关联疗程卡"选项）
│       ├── 选择美容师（底部 Popup）
│       └── 选择时间（日历 + 固定时段 selector）
├── 凤御馆 (cart)
│   └── 品牌宣传图（待完善为长图文）
└── 我的 (profile)
    ├── 绑定手机号（open-type="getPhoneNumber"）
    ├── 切换门店 → 门店列表 (store-select)
    ├── 疗程卡 (treatment-cards) → 预约 / 订单详情
    ├── 订单中心 (orders) → 订单详情 (order-detail)
    │   └── 去付款 / 取消 / 发起预约 → 疗程卡页
    ├── 个人信息 → 编辑页 (P1)
    ├── 优惠券 (P1)
    ├── 积分 (P2)
    ├── 消息 (P2)
    ├── 我的评价 (P2)
    ├── 服务记录 (P1)
    ├── 我的卡包 (P2)
    ├── 充值卡 (P2)
    ├── 意见反馈 (P2)
    ├── 客服热线 → 拨打电话
    └── 在线客服 (P2)

支付场景（checkout / scan-pay）统一支持：微信支付 / 支付宝 / 线下付款
```

---

## 6. 数据来源对照

### 只读查询（WorkFine SQL Server）

| 数据域 | WorkFine 表 | 用途 |
|--------|------------|------|
| 门店列表 | `UDT_M_219` | 门店选择与绑定 |
| 美容师列表 | `UDT_S_287` | 美容师浏览与选择 |
| 顾客档案 | `UDT_S_311` | 主美容师、顾客来源等参考信息 |
| 可售项目（全国） | `UDT_M_1281` | SKU 价格/次数实时读取 |
| 门店自定义项目 | `UDT_M_1383` | SKU 价格/次数实时读取 |
| 院装产品 | `UDT_M_341` | SKU 价格实时读取 |
| 品项分类 | `UDT_M_229` | 分类目录参考 |

### 读写（PG 自托管数据库）

| 数据域 | PG 表 | 用途 |
|--------|------|------|
| 微信用户 | `client_wechat_users` | 顾客身份 |
| 商品 SPU | `product_spu` | 商品元数据（名称、图片、分类、排序） |
| SKU 映射 | `product_spu_sku_map` | SPU↔WorkFine 项目编号映射 |
| 订单主表 | `orders` | 订单 CRUD |
| 订单明细 | `order_items` | 商品行、剩余次数、价格快照 |
| 营业额分配 | `revenue_allocations` + `revenue_allocation_items` | 自动/手动分配（顾客端只读） |
| 预约 | `appointments` | 预约 CRUD |
| 服务单 | `service_orders` + `service_items` | 顾客端只读查看 |

---

## 7. 环境约束（引用 real.md）

| 约束 | 描述 |
|------|------|
| 疗程次数原子扣减 | `UPDATE ... SET remaining_sessions = remaining_sessions - n WHERE remaining_sessions >= n`，禁止先 SELECT 后 UPDATE |
| 价格快照不可变 | 开单时写入 `unit_price`，后续价格变动不影响历史订单 |
| 支付幂等 | 微信回调、线下确认、服务完成接口均需幂等，不得重复入账或扣次 |
| 状态单向推进 | 订单/服务单/预约状态只能沿状态机正向流转；唯一例外：店长可重置 `支付失败` → `待支付` |
| 门店数据隔离 | 所有 PG 查询以 `ctx.auth.userId` 或 `ctx.auth.storeName` 过滤，禁止跨用户/跨门店访问 |
| 待支付订单唯一 | 同一顾客同时只能有一笔待支付订单（数据库部分唯一索引 + 应用层校验） |

---

## 8. 验收标准

### P0 核心功能

| ID | 标准 | 验证方式 |
|----|------|----------|
| AC-01 | 微信登录后自动创建用户记录，获取 openid | 查看 `client_wechat_users` 表 |
| AC-02 | 手机号绑定后，历史订单（`client_user_id IS NULL` + 手机号匹配）自动关联 | 员工先开单 → 顾客后注册 → 检查订单可见性 |
| AC-03 | 商品列表仅展示含有效 SKU 的 SPU | 将 SKU 全部设为 `is_active = false` → SPU 不应展示 |
| AC-04 | SKU 价格从 WorkFine 实时读取，与设计端一致 | 修改 WorkFine 价格 → 刷新后价格更新（5 分钟缓存窗口内） |
| AC-05 | 同一顾客不能创建第二笔待支付订单 | 连续提交两笔订单 → 第二笔被拒绝 |
| AC-06 | 微信支付成功后订单状态变为 `已支付` | 支付流程端到端测试 |
| AC-07 | 线下付款提交后状态变为 `待确认收款`，店长确认后变为 `已支付` | 线下付款端到端测试 |
| AC-08 | 疗程卡服务完成后 `remaining_sessions` 正确扣减 | 从 3 → 服务一次 → 变为 2 |
| AC-09 | 预约仅限 `remaining_sessions > 0` 且非院装产品的已支付订单行 | 院装产品订单行 → 预约按钮不应出现 |
| AC-10 | 同一订单行已有待确认/已确认预约时不可重复发起 | 重复创建 → 接口返回错误 |
| AC-11 | 扫码支付能正确加载员工端创建的订单并完成付款 | 员工开单 → 生成二维码 → 顾客扫码 → 支付成功 |
| AC-12 | 订单列表按状态 Tab 正确筛选 | 切换 Tab → 对应状态订单正确显示 |
| AC-15 | 门店搜索支持关键词匹配（≥2 字符，按 store_name 或 region） | 输入门店名关键字 → 匹配结果正确 |

### P1 重要功能

| ID | 标准 | 验证方式 |
|----|------|----------|
| AC-13 | 个人资料修改后立即生效 | 修改昵称 → 返回个人中心 → 显示新昵称 |
| AC-14 | 优惠券在下单时可选择并正确抵扣 | 选择优惠券 → 订单金额减少对应金额 |

---

## 9. 不包含功能（明确排除）

| 功能 | 排除原因 |
|------|----------|
| 订单转换（转换单） | 复杂度高，后续版本 |
| 退款/售后 | 需对接微信支付退款接口，后续版本 |
| 充值卡金抵扣 | 需建立充值卡金体系，后续版本 |
| 待转金管理 | 订单余额转换与退款，后续版本 |
| 内部单（员工消费） | 员工端功能，客户端不涉及 |
| 回款单 | 分期回款流程，后续版本 |
| 复杂提成计算 | 员工端功能 |
| 数据看板 | 员工端功能 |

---

## 10. clientApi 接口汇总

| 模块 | 接口 | 说明 |
|------|------|------|
| auth | login | 微信静默登录（自动创建用户记录） |
| auth | bindPhone | 绑定手机号（CloudID 解密 + 历史订单补全） |
| auth | bindStore | 绑定门店（含市场名自动提取） |
| store | list | 门店列表（按市场分组，支持城市筛选） |
| store | detail | 门店详情（含 staff_count、customer_count） |
| store | requestUnbind | 申请解绑门店 |
| store | getUnbindRequest | 查询解绑申请状态 |
| store | cancelUnbindRequest | 取消解绑申请 |
| store | geocode | 逆地理编码（腾讯地图 API） |
| product | categories | 品项分类列表（仅含有效 SKU 的分类） |
| product | spuList | SPU 商品列表（含 SKU 价格，市场限定过滤） |
| product | skuDetail | SKU 详情（WorkFine 价格，5 分钟缓存） |
| product | spuDetail | SPU 详情（促销方案含 promotionSchemeId） |
| product | hotList | 热门商品（排除院装产品和促销方案） |
| product | shopInit | 商城初始化数据（分类 + 首分类 SPU 列表） |
| staff | list | 美容师列表（按门店过滤） |
| staff | default | 默认美容师（顾客档案主美容师） |
| order | create | 创建订单（需手机号，10 分钟超时，advisory lock 防并发） |
| order | pay | 微信支付（当前 mock 模式） |
| order | alipayPay | 支付宝支付（当前 mock 模式） |
| order | offlinePay | 线下付款提交 |
| order | list | 订单列表（带懒清理过期订单） |
| order | detail | 订单详情（含 expireAt 计算） |
| order | cancel | 取消订单 |
| order | appointableItems | 可预约项目（支持 includeInactive） |
| order | scanDetail | 扫码订单详情（公开接口，仅 staff 订单） |
| appointment | create | 创建预约（支持不关联疗程卡） |
| appointment | list | 预约列表（LEFT JOIN 获取服务名） |
| appointment | cancel | 取消预约（支持取消原因） |
| service | detail | 服务单详情（只读） |
