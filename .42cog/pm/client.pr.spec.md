# 凤御双美容院 — 客户端小程序产品需求规格书

> **文档版本**: 2.0.0
> **端口**: 客户端小程序（C端，appid: wx811eb4ded3dfba3f）
> **角色**: 顾客
> **约束文档**: `.42cog/real.md` | `.42cog/cog.md`
> **参考设计稿**: `notes/客户端UI/`（MasterGo 原型，27 页截图）
> **日期**: 2026-03-09

---

## 1. 产品概述

**名称**: 凤御美业客户端小程序
**目标用户**: 凤御双美容院的顾客（C端消费者）

**核心价值**: 浏览并购买美容服务/产品 | 预约到店查看服务进度与疗程余次 | 扫码完成员工端开单的订单支付

**色彩方案**（白红主题）:
- 主色：中国红 `#C0322A`（按钮/Tab/高亮/购物车 FAB 等全局主色）
- 辅助：`#FFF0EE`（浅红背景）、`#F5F5F5`（侧边栏/搜索栏）、`#52C41A`（成功/加购）
- 订单状态：待支付 `#FAAD14` | 待确认收款 `#C9986A` | 已支付 `#52C41A` | 已完成/已关闭 `#8C8C8C` | 支付失败 `#FF4D4F`
- 预约状态：待确认 `#D48806` | 已确认 `#A0785A` | 已完成 `#389E0D` | 已取消/已关闭 `#8C8C8C`
- 文本：`#1A1A1A` → `#666666` → `#999999`；背景：`#FAFAFA`（页面）/ `#FFFFFF`（卡片）
- Vant 主题覆盖：所有组件主色统一为 `#C0322A`

---

## 2. 核心用户旅程

- **旅程 A 自助下单**: 浏览商品 → 选择 SKU → 选择美容师（可选）→ 提交订单 → 支付 → 完成
- **旅程 B 扫码支付**: 微信扫码 → 自动加载员工端订单 → 选择支付方式 → 完成支付
- **旅程 C 预约核销**: 疗程卡（余次 > 0）→ 发起预约 → 员工确认 → 到店签到 → 服务完成（扣减次数）

---

## 3. 功能需求

#### 3.1 认证与用户身份 ✅

| ID | 需求 | 说明 |
|----|------|------|
| AUTH-01 | 微信静默登录 | `app.onLaunch()` → `auth.login`；新用户自动创建（`FYGK-{YYYYMMDD}{序号}`）；先 localStorage 恢复再服务端同步 |
| AUTH-02 | 手机号绑定 | `open-type="getPhoneNumber"` → CloudID 解密；触发历史订单补全（匹配 `client_user_id IS NULL`）；已绑定手机号拒绝 |
| AUTH-03 | 门店绑定 | 写入 `bound_store_id`；市场名通过 JOIN `stores` → `org_nodes` 获取 |

**流程**: `onLaunch` → `auth.login`（静默）→ 需手机号时（-403 PHONE_REQUIRED）→ 个人中心绑定 → 清除 auth 缓存 → 重试

> 设计稿 screen_002 登录弹窗（手机号+验证码+协议）未实现，当前采用静默登录+CloudID，保留供参考。

---

#### 3.2 门店选择与绑定 ✅

| ID | 需求 | 说明 |
|----|------|------|
| STORE-01 | 门店列表 | 按市场分组；支持城市筛选 |
| STORE-02 | 门店详情 | 门店名、所属市场、床位、开业时间、`employee_count`、`customer_count` |
| STORE-03 | 绑定门店 | 首次选择后默认展示该门店商品/美容师 |
| STORE-04 | 更换门店 | 解绑申请（发起→查看状态→可取消） |
| STORE-05 | 地理定位 | `store.geocode`（腾讯地图逆地理编码，去掉"市"后缀）→ 推荐就近门店 |

- **搜索**: ≥2 字符，按 `store_name` 或 `region` 匹配
- **绑定状态 UI（4 种）**: `no-binding`（绑定按钮）| `is-current`（解绑按钮）| `is-current-reviewing`（审批中+取消）| `other-bound`（禁用）
- **解绑流程**: 输入原因 → `store.requestUnbind` → 审批中 → 可取消

---

#### 3.3 首页 ✅

**导航栏**: 品牌 Logo + 门店选择器 + 扫码（`wx.scanCode()`）

| 区域 | 说明 |
|------|------|
| Banner 轮播 | 5 张 CDN 图片，自动轮播 4s/张 |
| 金刚区 | 福利活动/护理项目/家居产品 → 滚动到对应分类；我的券 → Toast"开发中"；我的疗程 → treatment-cards |
| 产品专区 | 左侧分类导航（product_kind 标题不可点+子分类可点）+ 右侧商品卡片（联动滚动） |
| 购物车 FAB | badge（>99 显示 `99+`），仅 `cartCount > 0` 可见 |

**关键行为**: 搜索跨分类即时过滤（预加载未缓存分类按名称过滤，结果替代列表区）| 扫码 path 型 `navigateTo` / orderNo 型跳转 scan-pay | SPU 分页 scroll-to-lower 500ms 防抖+模块级缓存 | 切换门店清空购物车和缓存

---

#### 3.4 商品浏览与选购 ✅

**三层浏览**: ① 品项分类选择器（仅含有效 SKU；家居产品追加末尾）→ ② SPU 卡片列表（同商品合并，价格起步+生美/非生美标签）→ ③ 商品详情页（SKU 规格选择+数量+美容师 Popup+操作栏）

**分类标签着色**: 按 product_kind — 紫/主色/青/橙

**福利活动特殊规则**:

| 环节 | 常规商品 | 福利活动 |
|------|---------|----------|
| 操作栏 | 购物车 FAB + 加入购物车 + 立即下单 | 隐藏购物车，仅全宽"立即下单" |
| 购物车 | 可加入，多商品结算 | 不进购物车，单品直接下单 |
| 热门列表 | 包含 | 排除 |
| 下单类型 | 按商品类型确定 | `sale_order_type='福利活动'` + `promotionSchemeId` |

**购物车（localStorage）**: `utils/cart.ts` — CartItem: `{ skuId, spuId, spuName, skuDisplayName, coverImage, price, quantity, bigCategory, productType, addedAt }`

---

#### 3.5 美容师浏览 ✅

| ID | 需求 | 说明 |
|----|------|------|
| EMPLOYEE-01 | 美容师列表 | 按门店过滤在职员工，身份按 `skills` 数组判定：`is_resigned=false AND skills && ARRAY['美容师','养生师']`（养生师亦可被指定接单，不按 `position_name`） |
| EMPLOYEE-02 | 默认美容师 | `client_wechat_users.bound_employee_id`，预约/下单默认填充 |
| EMPLOYEE-03 | 不指定选项 | 下单时可不选 |

当前以底部 Popup 实现（详情页/结算页/预约页），含"不指定"+美容师列表。设计稿 screen_008 独立列表页待实现。

---

#### 3.6 下单与支付 ✅

**结算页三种入口**:

| 模式 | 触发 | 数据来源 |
|------|------|----------|
| 单品直接 | 详情页"立即下单" → URL 带 `skuId`+`quantity`+`EmployeeId` | `product.skuDetail` |
| 购物车结算 | 购物车"结算" → URL 带 `fromCart=1` | `localStorage['checkoutItems']` |
| 恢复支付 | 订单详情"去支付" → URL 带 `orderNo` | `order.detail` |

**支付方式**: 微信支付（mock）/ 支付宝（mock，QR 码 Popup）/ 线下付款（已实现）→ 线下付款流转：待支付 → 待确认收款 → 已支付

**手机号拦截**: `order.create` 返回 `-403 PHONE_REQUIRED` → 绑定 overlay → CloudID → 自动重试

**10 分钟超时**: `expire_at = created_at + 10min`；`order.list`/`order.create`/`order.pay` 时懒清理

**核心规则**: 前端防连点+后端幂等（见 `backend.pr.spec.md` §2.8）| 价格快照不可变（见 `real.md`）| 已支付+指定美容师→自动创建营业额分配 | 消费协议复选框（未勾选禁用提交）| 按钮文本随支付方式变化 | 状态集见 `backend.pr.spec.md` §5

---

#### 3.6.1 储值卡抵扣支付（跨店共享） ✅

**定位**: 储值卡是"**抵扣项**"（与优惠券并列），**不是支付方式**。下单页 UI 分为三段："订单总额 → 抵扣（优惠券 + 储值卡）→ 实付金额（支付方式通道）"。

**核心规则**:

| 规则 | 说明 |
|------|------|
| 跨店共享 | 一户一账户；`prepaid_cards.store_id` 列已 DROP；顾客换绑门店后原余额继续可用 |
| 抵扣开关默认开 | 有余额时默认 `useCard=true`，能抵多少抵多少；用户可手动关闭 |
| 实付 = 0 时 | `payment_method='无'`（`paymentMethodEnum` 已扩展为 4 值：微信/支付宝/线下/无）；UI 隐藏支付方式按钮组 |
| 金额字段 | `sale_orders.prepaid_card_amount`（抵扣额，不计入实付）+ `paid_amount`（走支付通道的实付）；`prepaid_card_amount + paid_amount = total_amount`（DB CHECK） |
| 不支持自定义抵扣金额 | UI 仅给开关，后端按 `min(balance, total - couponDiscount)` 自动算 |
| 余额不足 | 返回 `INSUFFICIENT_BALANCE`；前端弹框由用户决定（关抵扣重付 / 取消订单），不自动降级 |

**下单流程**:

1. `onLoad` 调 `card.balance` 拉跨店统一余额（无 storeId 参数）
2. 用户选券 + 决定是否用储值卡 → `order.create` 传 `useCard` + `prepaidCardAmount?`
3. 后端事务内 `SELECT balance FOR UPDATE` 基础校验，写入订单
4. 实付 = 0（全额抵扣）→ 同事务扣卡 + 订单直接 `'已支付'`（跳过 `order.pay`）
5. 实付 > 0 + 微信 → 唤起微信支付 → `payNotify` 成功回调时同事务扣卡
6. 实付 > 0 + 线下 → 订单转 `'待确认收款'`，员工端 `confirmOffline` 时扣卡

**扫码支付链路（员工预选 → 顾客确认）**:

- 店长在员工端预选的抵扣方案随订单存入（`prepaid_card_amount` 为预选值，`prepaid_cards.balance` 未动）
- 顾客扫码进 `scan-pay` 页 → 看到预填方案 → 可调整（关抵扣 / 部分抵扣 / 改支付方式）→ 调 `order.scanAdjust` 重算落库
- 顾客点"确认支付"：
  - `paid_amount = 0` → `order.confirmPrepaidFull`（事务内扣卡 + 置已支付）
  - `paid_amount > 0` → 按选的通道走（微信/线下）

**退款回冲**: 退款按比例拆分：`refundByCard = floor(prepaid/total × refund, 2)`，`refundByOrigin = refund − refundByCard`（反向相减保证无尾差）；储值卡部分 INSERT `card_transactions(type='充值')` + balance 回冲，原通道部分走原退款链路。

**API**: `card.balance`（查当前用户跨店统一余额）| `card.list` / `card.history`（余额与流水）| `order.create`（增补 `useCard` / `prepaidCardAmount`）| `order.scanAdjust`（扫码后调整预选方案）| `order.confirmPrepaidFull`（全额抵扣确认支付）

---

#### 3.7 订单管理 ✅

**列表**: Tab（全部/待支付/已支付/已完成）；体验单"体验"标签；待支付显示"去支付"；日期 `YYYY-M-D`

| 状态 | 顾客操作 |
|------|---------|
| 待支付 | 去付款、取消订单 |
| 待确认收款 | 等待 |
| 已支付 | 套餐明细、发起预约（有可预约项时） |
| 终态 | 查看详情 |

**详情关键行为**: 待支付倒计时（MM:SS，1s 刷新，超时自动重载）| 疗程卡（含原单品=1 次卡）"剩余 X/Y 次"+有效期，家居产品"已交付" | 底部按钮：待支付→取消+去支付；已支付有可预约项→发起预约→疗程卡页；终态→返回首页

**可预约项**: `remaining_sessions > 0` 且非家居产品；支持 `includeInactive`

---

#### 3.8 预约管理 ✅

**入口**: 预约 Tab（创建页）| 疗程卡"预约"按钮 | 订单详情→疗程卡页 | 个人中心→我的预约

**创建**: 选项目（内联 radio，含"不关联疗程卡"→`service_name`显示"到店预约"）| 选美容师（Popup）| 选时间（日历当日~+90天 + 5时段 09-11/11-13/13-15/15-17/17-19）| 备注（max 200）

**格式**: 时间 `"YYYY-MM-DD HH:MM-HH:MM"` | ID `apt_{timestamp}_{random}`

**列表**: Tab（全部/待确认/已确认/已取消）；LEFT JOIN `sale_items` + product 获取服务名

**约束**: 仅已支付+余次>0+非家居产品可预约 | 同一行有活跃预约不可重复 | 取消后可重新发起（系统关闭的不可）| 预约不扣次数

---

#### 3.9 到店服务 ✅

顾客端查看服务单状态（待服务/服务中/待客户确认/已完成）和服务信息。产品类型差异见 `cog.md`。

**顾客确认服务完成**：员工标记完成后服务单进入 `待客户确认`，顾客在「服务记录」列表对该单点「确认服务完成」（`service.confirm`），确认后才扣减卡剩余次数并完成服务单；确认后方可对美容师评价。店长 / 后台可代确认作兜底。

---

#### 3.10 扫码支付 ✅

**流程**: 员工开单生成二维码 → 顾客扫码 → 解析 QR（path 型 `navigateTo` / orderNo 型 scan-pay）→ `order.scanDetail`（仅 `sale_order_source='staff'`）→ 选择支付方式 → 完成

**约束**: 订单已入库（`待支付`），扫码不重复创建；`scanDetail` 无需认证。非待支付→`statusMsg` 浮层；无效码→友好提示

---

#### 3.11 疗程卡管理 ✅

已支付订单中的疗程卡行（含原单品=1 次卡）：项目名、剩余/总次数、到期日、状态。卡片可直接发起预约。

**到期日**: 疗程卡→合同约定（2026-05-21 单品合并后，原"单品 `paid_at + 1 year`"自动到期规则已移除）

---

#### 3.12 个人中心（部分实现）

**已实现**: 脱敏手机号（`138****7038`）、门店名、手机号绑定（成功提示"已同步 N 笔历史订单"）、我的订单、我的疗程卡、切换门店、版本号

**待实现**: 头像/昵称编辑、会员等级、四宫格（优惠券/积分/消息）、订单快捷入口（按状态）、我的预约、服务记录、充值卡、客服热线

---

#### 3.13 个人资料编辑（未实现）

screen_025：可编辑头像（相册/拍摄）+名称；只读编号+注册门店；电话+"换绑"（重新 `getPhoneNumber`）

---

#### 3.14 来源渠道绑定（未实现）

绑定门店页增加来源选择（推广部/老带新/美团/抖音/转让店/自进/内部地推/第三方拓客）+推广人（选填）。需增加 `source_channel`、`promoter` 字段。

---

#### 3.15 优惠券（未实现）

screen_026：Tab（未使用/已使用/已过期）+ 兑换码 + 券卡片

| 券种 | 本质 | 说明 |
|------|------|------|
| 现金券 | 直接抵扣 | 福利活动赠送 |
| 项目券 | 限定范围现金券 | 限定品项分类 |
| 折扣券 | 乘法计算 | 后续迭代 |

通用要素：满减条件、适用范围、适用门店。下单时展示可用券并计入价格明细。

---

#### 3.16 门店详情增强（部分实现）

screen_016 列表增强：搜索+排序（距离/评分/销量）+卡片增强（图片/评分/距离/月售/营业时间）

screen_014 详情增强：门店主图+营业信息+商品区（横向卡片）+推荐技师+评价（待规划）

---

#### 3.17 美容师详情增强（未实现）

screen_008：状态标签（空闲/忙碌，按当日预约计算）、擅长项目、评分+服务次数、"预约"按钮（预填美容师）。筛选：服务数量、擅长项目。

---

#### 3.18 服务记录（未实现）

screen_027：记录卡片（项目名+地点+技师+时间+时长）。入口：个人中心 → 服务记录。

---

#### 3.19 凤御馆 ✅

当前单张品牌宣传 CDN 图片。规划 screen_015：品牌简介→专家团队→资质证书长图文。

---

#### 3.20 积分系统（未实现）

screen_018：VIP 卡片（红色渐变，等级+积分）+积分商品列表（两列）+筛选。明细页 Tab（收支/兑换）+二级筛选。需新建积分表+会员等级体系。

---

#### 3.21 消息中心（未实现）

screen_024：系统消息列表（标题+摘要+时间+未读角标）。类型：预约提醒（蓝）、支付通知（绿）、优惠通知（橙）。

---

#### 3.22 充值卡（部分实现）

screen_027：卡内余额 + 消费明细。提示"仅显示最近半年"。

**已实现**: `card.list`（跨店统一余额）/ `card.history`（近 6 个月流水，含充值/扣款/退款回冲三类）/ `card.balance`（下单页抵扣用）；已接入下单抵扣链路（见 §3.6.1）。

**重要**: 余额**跨店共享**（`prepaid_cards` 已 DROP `store_id`），顾客换绑门店后原余额继续可用，不再按门店隔离。筛选项仅保留日期筛选。

---

## 4. 状态机

> 见 `backend.pr.spec.md` §5。顾客端仅可：取消订单（待支付→已关闭）、取消预约（待确认/已确认→已取消）、服务单只读。

---

## 5. 页面结构

### 5.1 TabBar

| Tab | 页面路径 | 说明 |
|-----|----------|------|
| 首页 | `pages/home/home` | 主页面 |
| 预约 | `pages/appointment/appointment` | 创建预约 |
| 凤御馆 | `pages/cart/cart` | 品牌介绍 |
| 我的 | `pages/profile/profile` | 个人中心 |

### 5.2 分包结构

| 分包 | 页面 | 说明 |
|------|------|------|
| pagesShop | shop、service-detail、shopping-cart | 商品模块 |
| pagesOrder | orders、checkout、order-detail、scan-pay、treatment-cards | 订单模块 |
| pagesStore | store-select、store-detail | 门店模块 |
| pagesAppointment | appointment-create | 预约模块 |

---

## 6. 数据来源

> 见 `backend.pr.spec.md` §2。

---

## 7. 环境约束

> 见 `real.md`。

---

## 8. 验收标准

| ID | 标准 | 验证方式 |
|----|------|----------|
| AC-01 | 微信登录后自动创建用户记录 | 查看 `client_wechat_users` 表 |
| AC-02 | 手机号绑定后历史订单自动关联 | 员工先开单→顾客后注册→检查可见性 |
| AC-03 | 仅展示含有效 SKU 的 SPU | SKU 全部 inactive → SPU 不展示 |
| AC-04 | SKU 价格与管理端一致 | 修改价格→刷新后更新 |
| AC-05 | 同一顾客不能创建第二笔待支付订单 | 连续提交→第二笔被拒绝 |
| AC-06 | 微信支付后状态变为已支付 | 端到端测试 |
| AC-07 | 线下付款→待确认→确认→已支付 | 端到端测试 |
| AC-08 | 服务完成后余次正确扣减 | 3→服务→2 |
| AC-09 | 预约仅限有余次+非家居产品+已支付 | 家居产品无预约按钮 |
| AC-10 | 有活跃预约时不可重复发起 | 重复→返回错误 |
| AC-11 | 扫码支付端到端 | 员工开单→二维码→扫码→支付 |
| AC-12 | 订单 Tab 正确筛选 | 切换验证 |
| AC-13 | 资料修改后立即生效 | 修改→显示新值 |
| AC-14 | 优惠券正确抵扣 | 选券→金额减少 |
| AC-15 | 门店搜索 ≥2 字符 | 关键字验证 |
