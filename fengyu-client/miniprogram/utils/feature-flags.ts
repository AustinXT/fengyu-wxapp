// utils/feature-flags.ts —— 临时功能开关

/**
 * ORDERS_ENTRY_ENABLED —— 订单「主动查看」入口总开关。
 *
 * 关闭范围（仅前端入口，后端云函数 order.* 不受影响）：
 *   1. 「我的」页「我的订单」入口（隐藏）
 *   2. 疗程卡列表点卡片进订单详情（拦截提示）
 *   3. 消息中心点订单消息进订单详情（拦截提示）
 *   4. 订单列表页兜底拦截（防遗漏入口直达）
 *
 * 不影响支付闭环：客户下单/支付完成后仍正常跳订单详情看结果（checkout / scan-pay 不动）。
 *
 * 业务运行平稳后（约 2026-07 月底）恢复：把下方改回 true 即可，无需改动其它任何文件。
 */
export const ORDERS_ENTRY_ENABLED = false;
