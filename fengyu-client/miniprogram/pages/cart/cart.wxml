<!-- pages/cart/cart.wxml — 凤御馆宣传页 -->
<view class="container">
  <image
    wx:for="{{strips}}"
    wx:key="index"
    class="strip-image"
    src="{{item.url}}"
    mode="widthFix"
    lazy-load
    style="height:{{item.heightRpx}}rpx"
  />
</view>
